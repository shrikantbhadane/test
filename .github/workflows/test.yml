# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    #branches: [ "main" ]
 
  # Allows you to run this workflow manually from the Actions tab
  #workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    #strategy:
    #  matrix:
    #    env: [test, qa]
    #environment:
    # name: ${{ matrix.env }}
    # url: https://github.com
    
    env:
      var1: ${{ github.ref }}
      var2: startsWith(github.ref, 'refs/heads/feature/')
      
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
      
      - name: map environment variables based on matix strategy
        uses: kanga333/variable-mapper@master
        with:
          key: "${{ matrix.env }}"
          map: |
           {
              "test": {
                "HELM_ARGS": "--set ingress.albsecuritygroups=sg-002e04ec1ab42fe7b\ sg-005695cc61fe146db\ sg-0320f9fa23244a652\ sg-0b42d7c69e3572496\ sg-0ed32713ebb19d40b"
                }
            }  
          
      # Runs a single command using the runners shell
      - name: Run a one-line script
        if: startsWith(github.ref, 'refs/heads/feature/')
        run: |
         #echo "This is a build job running in $var1"
         #echo "This is a build job running in ${{ vars.var }}"
         #helm version
         echo "Success"
         
  #test:
  #  runs-on: ubuntu-latest
  #  environment:
  #   name: qa
  #   url: https://github.com
    
    # Steps represent a sequence of tasks that will be executed as part of the job
  #  steps:
      # Runs a single command using the runners shell
  #    - name: Test one line
  #      run: echo "This is a test job running in ${{ vars.qa_var }}"

